# Demo of new features need to learn for Advanced Space job

## Need to use `backend` repo running on port 5000 as the Mongo DB

## Requirements
 - node v25

## To run
 - Do a `npm install`
 - From root, run `npm start`